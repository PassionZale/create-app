const REPO_DOWNLOAD_FOLDER = '.mp-starter-repo'

const ENV_BRANCH_MAPS = {
  dev: 'dev',
  sit: 'test',
  uat: 'pre-release',
  prod: 'master'
}

module.exports = {
  REPO_DOWNLOAD_FOLDER,
  ENV_BRANCH_MAPS
}
